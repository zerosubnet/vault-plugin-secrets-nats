ROM alpine:3.12 as downloader
ARG VERSION="1.5.2"
ARG OS="linux"
ARG ARCH="amd64"

COPY build/vault/plugins/vault-plugin-secrets-nats-${OS}-${ARCH} /vault-plugin-secrets-nats

COPY ./entrypoint.sh /entrypoint.sh

entrypoint ["/entrypoint.sh"]